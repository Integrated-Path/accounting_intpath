<odoo>
  <data>
    <!-- explicit list view definition -->
<!--
    <record model="ir.ui.view" id="accounting_intpath.list">
      <field name="name">accounting_intpath list</field>
      <field name="model">accounting_intpath.accounting_intpath</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
-->

    <!-- actions opening views on models -->
<!--
    <record model="ir.actions.act_window" id="accounting_intpath.action_window">
      <field name="name">accounting_intpath window</field>
      <field name="res_model">accounting_intpath.accounting_intpath</field>
      <field name="view_mode">tree,form</field>
    </record>
-->

    <!-- server action to the one above -->
<!--
    <record model="ir.actions.server" id="accounting_intpath.action_server">
      <field name="name">accounting_intpath server</field>
      <field name="model_id" ref="model_accounting_intpath_accounting_intpath"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": model._name,
        }
      </field>
    </record>
-->

    <!-- Top menu item -->
<!--
    <menuitem name="accounting_intpath" id="accounting_intpath.menu_root"/>
-->
    <!-- menu categories -->
<!--
    <menuitem name="Menu 1" id="accounting_intpath.menu_1" parent="accounting_intpath.menu_root"/>
    <menuitem name="Menu 2" id="accounting_intpath.menu_2" parent="accounting_intpath.menu_root"/>
-->
    <!-- actions -->
<!--
    <menuitem name="List" id="accounting_intpath.menu_1_list" parent="accounting_intpath.menu_1"
              action="accounting_intpath.action_window"/>
    <menuitem name="Server to list" id="accounting_intpath" parent="accounting_intpath.menu_2"
              action="accounting_intpath.action_server"/>
-->
  </data>
</odoo>